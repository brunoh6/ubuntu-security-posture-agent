def load_config(config_path=None):
    """
    Load agent configuration.

    For now, returns an empty config.
    Future versions may support file-based configuration.

    Args:
        config_path (str | None): Optional config file path

    Returns:
        dict: Configuration dictionary
    """
    return {}